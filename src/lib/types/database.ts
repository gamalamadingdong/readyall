/**
 * Supabase generated types — shared across LC, EL, and Hub.
 *
 * Regenerate with:
 *   npx supabase gen types typescript --project-id <PROJECT_ID> > src/lib/types/database.ts
 *
 * This is a placeholder. Copy the generated types from the LC repo
 * or regenerate from the shared Supabase project.
 */

export type Database = {
  // TODO: Replace with generated types from Supabase
  public: {
    Tables: Record<string, never>;
    Views: Record<string, never>;
    Functions: Record<string, never>;
    Enums: Record<string, never>;
  };
};
